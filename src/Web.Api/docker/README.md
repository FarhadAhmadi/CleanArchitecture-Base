Dockerfile variants for framework targets:

- `Dockerfile.net8`: build/runtime image pinned to .NET 8.
- `Dockerfile.net9`: build/runtime image pinned to .NET 9.

The default compose and Visual Studio flow use `src/Web.Api/Dockerfile`.
